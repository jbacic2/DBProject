plugins {
    id 'kotlin'
}

apply plugin: 'com.squareup.sqldelight'

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
    api "org.postgresql:postgresql:42.2.12"
    api "com.squareup.sqldelight:jdbc-driver:1.3.0"
}

sqldelight {
    BookDatabase {
        dialect = "mysql"
        packageName = "comp.databases.project.db"
    }
}